<UserControl x:Class="DistributedEnergySystem.Pages.Settings.TimeSettings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="700">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="20">

            <!-- 时间设置 -->
            <Border Background="#1F2937" CornerRadius="8" Margin="0,0,0,20" Padding="20">
                <StackPanel>
                    <TextBlock Text="🕐 时间设置" FontSize="16" FontWeight="SemiBold" Foreground="#E5E7EB" Margin="0,0,0,15"/>

                    <Border Background="#374151" CornerRadius="6" Padding="15,12">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="当前时间" FontSize="14" Foreground="#9CA3AF" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1" Name="CurrentTimeText" Text="2024-01-15 14:30:25"
                                          FontSize="14" Foreground="#10B981" VerticalAlignment="Center" Margin="20,0,0,0"/>
                            </Grid>

                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="时区设置" FontSize="14" Foreground="#9CA3AF" VerticalAlignment="Center"/>
                                <ComboBox Grid.Column="1" Name="TimeZoneComboBox" SelectedIndex="0" Width="150" Height="30"
                                         Background="#374151" Foreground="#E5E7EB"
                                         BorderThickness="0" Margin="20,0,0,0">
                                    <ComboBoxItem Content="北京时间 (UTC+8)"/>
                                    <ComboBoxItem Content="东京时间 (UTC+9)"/>
                                    <ComboBoxItem Content="伦敦时间 (UTC+0)"/>
                                    <ComboBoxItem Content="纽约时间 (UTC-5)"/>
                                </ComboBox>
                            </Grid>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="24小时制" FontSize="14" Foreground="#9CA3AF" VerticalAlignment="Center"/>
                                <ToggleButton Name="HourFormatToggle" Content="开启" IsChecked="True"
                                            Grid.Column="1" Width="60" Height="30"
                                            Background="#10B981" Foreground="White"
                                            BorderThickness="0" Margin="20,0,0,0"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="系统时间和时区配置" FontSize="12" Foreground="#6B7280" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <!-- 日期设置 -->
            <Border Background="#1F2937" CornerRadius="8" Margin="0,0,0,20" Padding="20">
                <StackPanel>
                    <TextBlock Text="📅 日期设置" FontSize="16" FontWeight="SemiBold" Foreground="#E5E7EB" Margin="0,0,0,15"/>

                    <Border Background="#374151" CornerRadius="6" Padding="15,12">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="日期格式" FontSize="14" Foreground="#9CA3AF" VerticalAlignment="Center"/>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="20,0,0,0">
                                    <RadioButton Name="DateFormat1Radio" Content="YYYY-MM-DD" IsChecked="True"
                                               Foreground="#E5E7EB" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                    <RadioButton Name="DateFormat2Radio" Content="DD/MM/YYYY"
                                               Foreground="#E5E7EB" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Grid>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="显示星期" FontSize="14" Foreground="#9CA3AF" VerticalAlignment="Center"/>
                                <ToggleButton Name="ShowWeekToggle" Content="开启" IsChecked="True"
                                            Grid.Column="1" Width="60" Height="30"
                                            Background="#10B981" Foreground="White"
                                            BorderThickness="0" Margin="20,0,0,0"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="日期显示格式配置" FontSize="12" Foreground="#6B7280" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <!-- NTP同步 -->
            <Border Background="#1F2937" CornerRadius="8" Margin="0,0,0,20" Padding="20">
                <StackPanel>
                    <TextBlock Text="🌐 NTP同步" FontSize="16" FontWeight="SemiBold" Foreground="#E5E7EB" Margin="0,0,0,15"/>

                    <Border Background="#374151" CornerRadius="6" Padding="15,12">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="自动同步时间" FontSize="14" Foreground="#9CA3AF" VerticalAlignment="Center"/>
                                <ToggleButton Name="NtpSyncToggle" Content="开启" IsChecked="True"
                                            Grid.Column="1" Width="60" Height="30"
                                            Background="#10B981" Foreground="White"
                                            BorderThickness="0" Margin="20,0,0,0"/>
                            </Grid>

                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="NTP服务器" FontSize="14" Foreground="#9CA3AF" VerticalAlignment="Center"/>
                                <TextBox Grid.Column="1" Name="NtpServerTextBox" Text="pool.ntp.org"
                                        Background="#1F2937" Foreground="#E5E7EB" BorderThickness="1"
                                        BorderBrush="#4B5563" Padding="8,4" Height="30"/>
                            </Grid>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="同步间隔" FontSize="14" Foreground="#9CA3AF" VerticalAlignment="Center"/>
                                <ComboBox Grid.Column="1" Name="SyncIntervalComboBox" SelectedIndex="2" Width="120" Height="30"
                                         Background="#374151" Foreground="#E5E7EB"
                                         BorderThickness="0" Margin="20,0,0,0">
                                    <ComboBoxItem Content="每小时"/>
                                    <ComboBoxItem Content="每6小时"/>
                                    <ComboBoxItem Content="每天"/>
                                    <ComboBoxItem Content="每周"/>
                                </ComboBox>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="网络时间协议同步设置" FontSize="12" Foreground="#6B7280" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <!-- 夏令时设置 -->
            <Border Background="#1F2937" CornerRadius="8" Padding="20">
                <StackPanel>
                    <TextBlock Text="☀️ 夏令时设置" FontSize="16" FontWeight="SemiBold" Foreground="#E5E7EB" Margin="0,0,0,15"/>

                    <Border Background="#374151" CornerRadius="6" Padding="15,12">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="自动夏令时" FontSize="14" Foreground="#9CA3AF" VerticalAlignment="Center"/>
                                <ToggleButton Name="DaylightToggle" Content="关闭" IsChecked="False"
                                            Grid.Column="1" Width="60" Height="30"
                                            Background="#374151" Foreground="#9CA3AF"
                                            BorderThickness="0" Margin="20,0,0,0"/>
                            </Grid>

                            <TextBlock Text="系统未启用夏令时功能" FontSize="12" Foreground="#6B7280"/>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="夏令时自动调整设置" FontSize="12" Foreground="#6B7280" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>