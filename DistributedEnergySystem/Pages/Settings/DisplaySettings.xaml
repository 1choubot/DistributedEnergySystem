<UserControl x:Class="DistributedEnergySystem.Pages.Settings.DisplaySettings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="700">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="20">

            <!-- 背光亮度 -->
            <Border Background="#1F2937" CornerRadius="8" Margin="0,0,0,20" Padding="20">
                <StackPanel>
                    <TextBlock Text="💡 背光亮度" FontSize="16" FontWeight="SemiBold" Foreground="#E5E7EB" Margin="0,0,0,15"/>

                    <Border Background="#374151" CornerRadius="6" Padding="15,12">
                        <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="200"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="亮度调节" FontSize="14" Foreground="#9CA3AF" VerticalAlignment="Center"/>
                                <StackPanel Grid.Column="1">
                                    <Slider Name="BrightnessSlider" Minimum="20" Maximum="100" Value="80"
                                             Foreground="#10B981" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding ElementName=BrightnessSlider, Path=Value, StringFormat={}{0:F0}%}"
                                               FontSize="12" Foreground="#6B7280" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Grid>

                            <Grid Margin="0,15,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="快速设置" FontSize="14" Foreground="#9CA3AF" VerticalAlignment="Center"/>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,0">
                                    <Button Name="BrightnessLowButton" Content="暗淡" Width="50" Height="25"
                                            Background="#374151" Foreground="#9CA3AF"
                                            BorderThickness="0" Margin="0,0,5,0" FontSize="11"/>
                                    <Button Name="BrightnessMediumButton" Content="标准" Width="50" Height="25"
                                            Background="#10B981" Foreground="White"
                                            BorderThickness="0" Margin="0,0,5,0" FontSize="11"/>
                                    <Button Name="BrightnessHighButton" Content="明亮" Width="50" Height="25"
                                            Background="#374151" Foreground="#9CA3AF"
                                            BorderThickness="0" FontSize="11"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="调节屏幕背光亮度" FontSize="12" Foreground="#6B7280" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <!-- 自动背光 -->
            <Border Background="#1F2937" CornerRadius="8" Margin="0,0,0,20" Padding="20">
                <StackPanel>
                    <TextBlock Text="🌙️ 自动背光" FontSize="16" FontWeight="SemiBold" Foreground="#E5E7EB" Margin="0,0,0,15"/>

                    <Border Background="#374151" CornerRadius="6" Padding="15,12">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="环境光感应" FontSize="14" Foreground="#9CA3AF" VerticalAlignment="Center"/>
                                <ToggleButton Name="AutoBacklightToggle" Content="开启" IsChecked="True"
                                            Grid.Column="1" Width="60" Height="30"
                                            Background="#10B981" Foreground="White"
                                            BorderThickness="0" Margin="20,0,0,0"/>
                            </Grid>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="感应灵敏度" FontSize="14" Foreground="#9CA3AF" VerticalAlignment="Center"/>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="20,0,0,0">
                                    <RadioButton Name="SensitivityLowRadio" Content="低"
                                               Foreground="#E5E7EB" Margin="0,0,15,0" VerticalAlignment="Center"/>
                                    <RadioButton Name="SensitivityMediumRadio" Content="中" IsChecked="True"
                                               Foreground="#E5E7EB" Margin="0,0,15,0" VerticalAlignment="Center"/>
                                    <RadioButton Name="SensitivityHighRadio" Content="高"
                                               Foreground="#E5E7EB" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="根据环境光自动调节亮度" FontSize="12" Foreground="#6B7280" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <!-- 息屏时间 -->
            <Border Background="#1F2937" CornerRadius="8" Margin="0,0,0,20" Padding="20">
                <StackPanel>
                    <TextBlock Text="⏰️ 息屏时间" FontSize="16" FontWeight="SemiBold" Foreground="#E5E7EB" Margin="0,0,0,15"/>

                    <Border Background="#374151" CornerRadius="6" Padding="15,12">
                        <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="自动息屏" FontSize="14" Foreground="#9CA3AF" VerticalAlignment="Center"/>
                                <StackPanel Grid.Column="1">
                                    <ComboBox Name="SleepTimeComboBox" SelectedIndex="2" Width="120" Height="30"
                                             Background="#374151" Foreground="#E5E7EB"
                                             BorderThickness="0" Margin="20,0,0,0">
                                        <ComboBoxItem Content="30秒"/>
                                        <ComboBoxItem Content="1分钟"/>
                                        <ComboBoxItem Content="5分钟"/>
                                        <ComboBoxItem Content="10分钟"/>
                                        <ComboBoxItem Content="永不"/>
                                    </ComboBox>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="长时间无操作后自动息屏" FontSize="12" Foreground="#6B7280" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <!-- 屏幕校准 -->
            <Border Background="#1F2937" CornerRadius="8" Margin="0,0,0,20" Padding="20">
                <StackPanel>
                    <TextBlock Text="📏 屏幕校准" FontSize="16" FontWeight="SemiBold" Foreground="#E5E7EB" Margin="0,0,0,15"/>

                    <Border Background="#374151" CornerRadius="6" Padding="15,12">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="触摸屏校准" FontSize="14" Foreground="#9CA3AF" VerticalAlignment="Center"/>
                                <Button Name="CalibrateButton" Content="开始校准"
                                        Grid.Column="1" Width="80" Height="30"
                                        Background="#10B981" Foreground="White"
                                        BorderThickness="0" Margin="20,0,0,0"/>
                            </Grid>

                            <TextBlock Text="校准触摸屏以提高操作精度" FontSize="12" Foreground="#6B7280"/>
                            <TextBlock Text="校准过程中请按照屏幕提示操作" FontSize="12" Foreground="#6B7280"/>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="触摸屏精度设置" FontSize="12" Foreground="#6B7280" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <!-- 节能模式 -->
            <Border Background="#1F2937" CornerRadius="8" Padding="20">
                <StackPanel>
                    <TextBlock Text="🔋 节能模式" FontSize="16" FontWeight="SemiBold" Foreground="#E5E7EB" Margin="0,0,0,15"/>

                    <Border Background="#374151" CornerRadius="6" Padding="15,12">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="自动待机模式" FontSize="14" Foreground="#9CA3AF" VerticalAlignment="Center"/>
                                <ToggleButton Name="PowerSaveToggle" Content="开启" IsChecked="True"
                                            Grid.Column="1" Width="60" Height="30"
                                            Background="#10B981" Foreground="White"
                                            BorderThickness="0" Margin="20,0,0,0"/>
                            </Grid>

                            <TextBlock Text="低功耗运行模式" FontSize="12" Foreground="#6B7280"/>
                            <TextBlock Text="降低亮度并关闭非必要功能" FontSize="12" Foreground="#6B7280"/>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="延长电池使用寿命" FontSize="12" Foreground="#6B7280" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>