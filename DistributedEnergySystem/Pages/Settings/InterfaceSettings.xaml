<UserControl x:Class="DistributedEnergySystem.Pages.Settings.InterfaceSettings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="700">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="20">

            <!-- 语言设置 -->
            <Border Background="#1F2937" CornerRadius="8" Margin="0,0,0,20" Padding="20">
                <StackPanel>
                    <TextBlock Text="🌐 语言设置" FontSize="16" FontWeight="SemiBold" Foreground="#E5E7EB" Margin="0,0,0,15"/>

                    <Border Background="#374151" CornerRadius="6" Padding="15,12">
                        <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="界面语言" FontSize="14" Foreground="#9CA3AF" VerticalAlignment="Center"/>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="20,0,0,0">
                                    <RadioButton Name="LangChineseRadio" Content="中文" IsChecked="True"
                                               Foreground="#E5E7EB" Margin="0,0,20,0" VerticalAlignment="Center"/>
                                    <RadioButton Name="LangEnglishRadio" Content="English"
                                               Foreground="#E5E7EB" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="选择系统显示语言" FontSize="12" Foreground="#6B7280" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <!-- 主题设置 -->
            <Border Background="#1F2937" CornerRadius="8" Margin="0,0,0,20" Padding="20">
                <StackPanel>
                    <TextBlock Text="🎨 主题设置" FontSize="16" FontWeight="SemiBold" Foreground="#E5E7EB" Margin="0,0,0,15"/>

                    <Border Background="#374151" CornerRadius="6" Padding="15,12">
                        <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="界面主题" FontSize="14" Foreground="#9CA3AF" VerticalAlignment="Center"/>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="20,0,0,0">
                                    <RadioButton Name="ThemeDarkRadio" Content="深色主题" IsChecked="True"
                                               Foreground="#E5E7EB" Margin="0,0,20,0" VerticalAlignment="Center"/>
                                    <RadioButton Name="ThemeLightRadio" Content="浅色主题"
                                               Foreground="#E5E7EB" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="选择界面配色方案" FontSize="12" Foreground="#6B7280" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <!-- 界面缩放 -->
            <Border Background="#1F2937" CornerRadius="8" Margin="0,0,0,20" Padding="20">
                <StackPanel>
                    <TextBlock Text="🔍 界面缩放" FontSize="16" FontWeight="SemiBold" Foreground="#E5E7EB" Margin="0,0,0,15"/>

                    <Border Background="#374151" CornerRadius="6" Padding="15,12">
                        <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="字体大小" FontSize="14" Foreground="#9CA3AF" VerticalAlignment="Center"/>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="20,0,0,0">
                                    <RadioButton Name="FontSmallRadio" Content="小"
                                               Foreground="#E5E7EB" Margin="0,0,15,0" VerticalAlignment="Center"/>
                                    <RadioButton Name="FontMediumRadio" Content="中" IsChecked="True"
                                               Foreground="#E5E7EB" Margin="0,0,15,0" VerticalAlignment="Center"/>
                                    <RadioButton Name="FontLargeRadio" Content="大"
                                               Foreground="#E5E7EB" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Grid>

                            <Grid Margin="0,15,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="200"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="缩放比例" FontSize="14" Foreground="#9CA3AF" VerticalAlignment="Center"/>
                                <StackPanel Grid.Column="1">
                                    <Slider Name="ZoomSlider" Minimum="80" Maximum="120" Value="100"
                                             Foreground="#10B981" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding ElementName=ZoomSlider, Path=Value, StringFormat={}{0:F0}%}"
                                               FontSize="12" Foreground="#6B7280" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="调整界面显示大小" FontSize="12" Foreground="#6B7280" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <!-- 动画效果 -->
            <Border Background="#1F2937" CornerRadius="8" Margin="0,0,0,20" Padding="20">
                <StackPanel>
                    <TextBlock Text="✨ 动画效果" FontSize="16" FontWeight="SemiBold" Foreground="#E5E7EB" Margin="0,0,0,15"/>

                    <Border Background="#374151" CornerRadius="6" Padding="15,12">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="界面动画" FontSize="14" Foreground="#9CA3AF" VerticalAlignment="Center"/>
                                <ToggleButton Name="AnimationToggle" Content="开启" IsChecked="True"
                                            Grid.Column="1" Width="60" Height="30"
                                            Background="#10B981" Foreground="White"
                                            BorderThickness="0" Margin="20,0,0,0"/>
                            </Grid>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="过渡效果" FontSize="14" Foreground="#9CA3AF" VerticalAlignment="Center"/>
                                <ToggleButton Name="TransitionToggle" Content="开启" IsChecked="True"
                                            Grid.Column="1" Width="60" Height="30"
                                            Background="#10B981" Foreground="White"
                                            BorderThickness="0" Margin="20,0,0,0"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="开启或关闭界面动画效果" FontSize="12" Foreground="#6B7280" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <!-- 预览区域 -->
            <Border Background="#1F2937" CornerRadius="8" Padding="20">
                <StackPanel>
                    <TextBlock Text="👁️ 预览效果" FontSize="16" FontWeight="SemiBold" Foreground="#E5E7EB" Margin="0,0,0,15"/>

                    <Border Background="#111827" CornerRadius="6" Padding="20" Height="120">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="预览文本示例" FontSize="14" Foreground="#E5E7EB" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <Button Content="预览按钮" Width="100" Height="30" Background="#10B981" Foreground="White"
                                   BorderThickness="0">
                                <Button.Resources>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                                           CornerRadius="4"
                                                           BorderThickness="{TemplateBinding BorderThickness}"
                                                           BorderBrush="{TemplateBinding BorderBrush}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Resources>
                            </Button>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="查看设置效果的实际显示" FontSize="12" Foreground="#6B7280" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>