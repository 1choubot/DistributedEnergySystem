<local:BasePage x:Class="DistributedEnergySystem.Pages.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:DistributedEnergySystem.Pages"
      xmlns:vm="clr-namespace:DistributedEnergySystem.ViewModels"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="1024"
      Title="系统设置">

    <Page.DataContext>
        <vm:MainWindowViewModel/>
    </Page.DataContext>

    <Grid>
        <!-- 背景装饰 -->
        <Grid.Background>
            <DrawingBrush>
                <DrawingBrush.Drawing>
                    <DrawingGroup>
                        <GeometryDrawing Brush="#0F1419">
                            <GeometryDrawing.Geometry>
                                <RectangleGeometry Rect="0,0,1,1"/>
                            </GeometryDrawing.Geometry>
                        </GeometryDrawing>
                        <GeometryDrawing>
                            <GeometryDrawing.Brush>
                                <SolidColorBrush Color="#10B981" Opacity="0.05"/>
                            </GeometryDrawing.Brush>
                            <GeometryDrawing.Geometry>
                                <GeometryGroup>
                                    <LineGeometry StartPoint="0,0.5" EndPoint="1,0.5"/>
                                    <LineGeometry StartPoint="0.5,0" EndPoint="0.5,1"/>
                                </GeometryGroup>
                            </GeometryDrawing.Geometry>
                        </GeometryDrawing>
                    </DrawingGroup>
                </DrawingBrush.Drawing>
            </DrawingBrush>
        </Grid.Background>

        <Grid.RowDefinitions>
            <!-- 主内容区域 -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 主内容区域 -->
        <Grid Grid.Row="0" Margin="20,20,20,20">
            <Grid.ColumnDefinitions>
                <!-- 左侧菜单 -->
                <ColumnDefinition Width="250"/>
                <!-- 分隔线 -->
                <ColumnDefinition Width="1"/>
                <!-- 右侧内容 -->
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧设置菜单 -->
            <Border Grid.Column="0" Background="#1A202C" CornerRadius="12" Margin="0,0,10,0">
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="2" Opacity="0.2"/>
                </Border.Effect>
                <StackPanel Margin="10">
                    <!-- 菜单标题 -->
                    <TextBlock Text="设置分类"
                               FontSize="16" FontWeight="SemiBold"
                               Foreground="#E5E7EB"
                               Margin="15,20,15,10"/>

                    <Separator Background="#374151" Margin="15,0,15,10"/>

                    <!-- 菜单项 -->
                    <ListBox Name="SettingsMenu"
                             Background="Transparent"
                             BorderThickness="0"
                             SelectionMode="Single"
                             SelectionChanged="SettingsMenu_SelectionChanged">

                        <!-- 界面设置 -->
                        <ListBoxItem Name="InterfaceSettingsItem"
                                     Background="Transparent"
                                     Margin="5,2"
                                     Tag="InterfaceSettings">
                            <StackPanel Orientation="Horizontal" Margin="10,8">
                                <TextBlock Text="🎨" FontSize="18" Margin="0,0,12,0"/>
                                <TextBlock Text="界面设置" FontSize="14" Foreground="#E5E7EB"/>
                            </StackPanel>
                        </ListBoxItem>

                        <!-- 时间设置 -->
                        <ListBoxItem Name="TimeSettingsItem"
                                     Background="Transparent"
                                     Margin="5,2"
                                     Tag="TimeSettings">
                            <StackPanel Orientation="Horizontal" Margin="10,8">
                                <TextBlock Text="🕒" FontSize="18" Margin="0,0,12,0"/>
                                <TextBlock Text="时间设置" FontSize="14" Foreground="#E5E7EB"/>
                            </StackPanel>
                        </ListBoxItem>

                        <!-- 音频设置 -->
                        <ListBoxItem Name="AudioSettingsItem"
                                     Background="Transparent"
                                     Margin="5,2"
                                     Tag="AudioSettings">
                            <StackPanel Orientation="Horizontal" Margin="10,8">
                                <TextBlock Text="🔊" FontSize="18" Margin="0,0,12,0"/>
                                <TextBlock Text="音频设置" FontSize="14" Foreground="#E5E7EB"/>
                            </StackPanel>
                        </ListBoxItem>

                        <!-- 显示设置 -->
                        <ListBoxItem Name="DisplaySettingsItem"
                                     Background="Transparent"
                                     Margin="5,2"
                                     Tag="DisplaySettings">
                            <StackPanel Orientation="Horizontal" Margin="10,8">
                                <TextBlock Text="🖥️" FontSize="18" Margin="0,0,12,0"/>
                                <TextBlock Text="显示设置" FontSize="14" Foreground="#E5E7EB"/>
                            </StackPanel>
                        </ListBoxItem>

                        <!-- 系统设置 -->
                        <ListBoxItem Name="SystemSettingsItem"
                                     Background="Transparent"
                                     Margin="5,2"
                                     Tag="SystemSettings">
                            <StackPanel Orientation="Horizontal" Margin="10,8">
                                <TextBlock Text="⚙️" FontSize="18" Margin="0,0,12,0"/>
                                <TextBlock Text="系统设置" FontSize="14" Foreground="#E5E7EB"/>
                            </StackPanel>
                        </ListBoxItem>

                        <!-- 安全设置 -->
                        <ListBoxItem Name="SecuritySettingsItem"
                                     Background="Transparent"
                                     Margin="5,2"
                                     Tag="SecuritySettings">
                            <StackPanel Orientation="Horizontal" Margin="10,8">
                                <TextBlock Text="🔐" FontSize="18" Margin="0,0,12,0"/>
                                <TextBlock Text="安全设置" FontSize="14" Foreground="#E5E7EB"/>
                            </StackPanel>
                        </ListBoxItem>

                        <!-- 连接设置 -->
                        <ListBoxItem Name="ConnectionSettingsItem"
                                     Background="Transparent"
                                     Margin="5,2"
                                     Tag="ConnectionSettings">
                            <StackPanel Orientation="Horizontal" Margin="10,8">
                                <TextBlock Text="📱" FontSize="18" Margin="0,0,12,0"/>
                                <TextBlock Text="连接设置" FontSize="14" Foreground="#E5E7EB"/>
                            </StackPanel>
                        </ListBoxItem>

                        <!-- 系统信息 -->
                        <ListBoxItem Name="SystemInfoItem"
                                     Background="Transparent"
                                     Margin="5,2"
                                     Tag="SystemInfo">
                            <StackPanel Orientation="Horizontal" Margin="10,8">
                                <TextBlock Text="ℹ️" FontSize="18" Margin="0,0,12,0"/>
                                <TextBlock Text="系统信息" FontSize="14" Foreground="#E5E7EB"/>
                            </StackPanel>
                        </ListBoxItem>
                    </ListBox>
                </StackPanel>
            </Border>

            <!-- 分隔线 -->
            <Border Grid.Column="1" Background="#374151" Margin="5,0"/>

            <!-- 右侧内容区域 -->
            <Border Grid.Column="2" Background="#1A202C" CornerRadius="12" Margin="10,0,0,0">
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="2" Opacity="0.2"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- 内容标题 -->
                    <StackPanel Grid.Row="0" Margin="30,25,30,20">
                        <TextBlock Name="ContentTitle"
                                   Text="设备配置"
                                   FontSize="20" FontWeight="SemiBold"
                                   Foreground="#E5E7EB"/>
                        <TextBlock Name="ContentDescription"
                                   Text="配置和管理系统设备参数"
                                   FontSize="12"
                                   Foreground="#9CA3AF"
                                   Margin="0,5,0,0"/>
                        <Separator Background="#374151" Margin="0,15,0,0"/>
                    </StackPanel>

                    <!-- 动态内容区域 -->
                    <Frame Name="ContentFrame"
                            Grid.Row="1"
                            NavigationUIVisibility="Hidden"
                            Background="Transparent"
                            Margin="25,0,25,25"
                            Content="设置内容将在这里显示..."/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</local:BasePage>